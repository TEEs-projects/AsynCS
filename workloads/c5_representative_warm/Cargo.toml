[package]
name = "asyncs-c5-representative-warm"
version = "0.1.0"
edition = "2021"

[dependencies]
flate2 = { version = "=1.1.9", default-features = false, features = ["rust_backend"] }
sha2 = "=0.10.9"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
